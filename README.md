# Dance Charleston landing page

A lightweight static landing page for the public Dance Charleston Google Calendar.

## Local preview

From this directory, run:

```powershell
python -m http.server 8000
```

Then open `http://localhost:8000`.

The site is intentionally separate from the operations dashboard in `../dashboard`.
